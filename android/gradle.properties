org.gradle.jvmargs=-Xmx4G
android.useAndroidX=true
android.enableJetifier=false
# Jetifier is disabled because it fails transforming some dependencies compiled
# with very new Java class versions (Unsupported class file major version 68).
# Disabling prevents the Jetifier transform run during builds. If you depend on
# older support libraries that require Jetifier, consider forcing compatible
# dependency versions or upgrading the Android Gradle Plugin/Jetifier tooling.
kotlin_version=2.1.0
agp_version=8.6.0
android.defaults.namespace=com.follow.zhuque
